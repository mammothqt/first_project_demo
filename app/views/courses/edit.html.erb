<h1><%= t(".title") %></h1>

<%= render 'form', category: @category, course: @course %>

<%= link_to t(".back"), category_course_path(@category, @course) %> |
<%= link_to t(".courses"), category_courses_path(@category) %>
