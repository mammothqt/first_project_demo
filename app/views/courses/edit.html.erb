<h1><%= "Edit" %></h1>

<%= render 'form', category: @category, course: @course %>

<%= link_to "Back", category_course_path(@category, @course) %> |
<%= link_to "Courses", category_courses_path(@category) %>
